source: github
repo: meshcore-go/meshcore-go
updatedAt: '2026-09-07T09:04:57.593Z'
releases:
  - version: v1.3.0
    name: v1.3.0
    datetime: '2026-09-07T05:35:52Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.3.0
    prerelease: false
    notes: >-
      Firmware fidelity release for the KISS modem, node timing and relay behaviour. Everything
      below was checked against MeshCore firmware `companion-v1.17.1`.


      ## Upgrade notes


      - **Go 1.26.7 or newer is required.** The `go` directive moved from 1.26.1 to 1.26.7 in all
      three modules. With `GOTOOLCHAIN=local` on an older 1.26.x that is a hard error rather than an
      automatic toolchain download.

      - **`hardware.DefaultTxTimeout` is now 15s, up from 5s.** The old value was shorter than the
      firmware's own worst-case transmit budget from SF11 up, so sends that had in fact gone out
      were reported as timeouts. For a tighter bound, pass `hardware.WithTxAirtimeEstimator` and the
      wait is derived from the firmware budget: TXDELAY + 1.5x airtime(255) + 1.5x airtime(len) +
      1s.

      - **A flood packet is re-flooded after local dispatch**, not during routing. A handler that
      calls `Packet.MarkDoNotRetransmit` now suppresses the relay.

      - **`hardware.WithHandlerWorkers` parallelizes non-DATA callbacks only.** DATA callbacks stay
      serial and in receive order on the drain goroutine, which node handlers require. If you used
      the worker pool to isolate a slow data handler, move that work to your own goroutine.

      - **`KissModem.Connect` always pushes the signal-report setting.** Firmware enables RX
      metadata by default, so a modem built without `WithSignalReport` previously left it streaming
      metadata that was then discarded.

      - An unresolved transmit blocks later sends with `ErrTxPending` until the firmware reports,
      and `Connect` clears it. An interrupted send returns `ErrModemClosed` or `ErrDisconnected`
      rather than success.


      ## Fixed


      - **Two nodes on one radio relayed each other's traffic.** Dedup caches are per-`Node`, so a
      packet one node originated was re-flooded by another node sharing the same modem once a
      neighbour bounced it back, seen as one antenna sending an advert at 0 hops and again at 2
      hops. `RadioMux` now records what it transmits and flags a returning copy do-not-retransmit,
      reproducing the firmware's markSeen-on-send at device granularity. Found in OwlShack bench
      testing.

      - **`Packet.AppendPathHash` corrupted parsed payloads.** It grew a `Path` slice that aliases
      the buffer the packet was parsed from, overwriting the first payload byte.

      - **A timed-out or failed transmit stranded the modem**, refusing every later send until the
      process restarted.

      - **`TX_DONE` could be lost** to inbound queue overflow, and a data handler calling `SendData`
      could deadlock. Completion now runs before user callbacks and bypasses the queue.

      - **Large hardware responses were dropped.** The transport resynced at 512 bytes, below the
      firmware's 1,030-byte encoded maximum, so a heavily escaped reply split across reads was
      discarded.

      - **TRACE bypassed the router**, so it was neither relayed nor counted.

      - **Airtime estimates were low.** `LoRaAirtimeEstimator` assumed an 8-symbol preamble;
      firmware uses 32 at SF5 to SF8 and 16 above.

      - **Hardware error frames were silent**, so a rejected command timed out instead of reporting
      why.


      ## Added


      `node`:


      - `WithRxDelay` with `RxDelayForScore` reproduces the firmware's `Dispatcher::calcRxDelay`,
      holding a weakly received flood packet so a stronger relay wins the channel. Pair it with
      `hardware.PacketScore`.

      - `WithFloodRetransmitDelay`, `WithDirectRetransmitDelay` and `WithExtraAckTransmitCount`.

      - TRACE relay with our SNR appended at `TracePriority`, plus multipart ACK relay and local
      delivery.

      - `Node.RouteStats` reporting received, duplicate, relayed and delivered counts, split by
      flood and direct.

      - `RegionMap.IsWildcard`, which `Wildcard` cannot do because it returns a copy.


      `hardware`:


      - `PacketScore`, matching `RadioLibWrapper::packetScoreInt`.

      - `KissModem.SendKissCommand` for the standard KISS parameters (TXDELAY, PERSISTENCE,
      SLOTTIME, TXTAIL, FULLDUPLEX), and `KissModem.GetMCUTemp`.

      - `HwErrorFor`, with `ModemStats.HwErrors` and `ModemStats.TxOutcomeLost`.

      - `ErrTxPending`, `ErrModemClosed`, `ErrDisconnected` and `Er

      …
  - version: v1.2.0
    name: v1.2.0
    datetime: '2026-09-03T03:40:18Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.2.0
    prerelease: false
    notes: >-
      Tracks MeshCore firmware **v1.17.1**. This release is mostly interoperability repair: several
      companion commands and responses did not match what the firmware actually sends, and a node
      behind a companion radio was putting undecodable packets on air. Every fix was derived by
      reading the firmware source side by side with this library, and each behavioural change
      carries a test.


      ## Breaking changes


      Read this section before upgrading.


      - **`node.Peer.OutPath` is now stored in send order.** Relays append their hash as a flood
      advert travels towards us, but a direct packet consumes its path from the front, so the
      previously stored route addressed the wrong first hop. `OutPathHashSize` is recorded alongside
      it, and the new `node.ReverseHops` converts between the two orders. If you persist advert
      paths, note which convention your column holds.

      - **Direct packets with hops remaining are no longer delivered to local handlers.** They are
      relayed only, matching `Mesh::onRecvPacket`. Handlers previously saw relay traffic.

      - **`node.Option` changed its underlying type** to `func(*nodeConfig)`. Every `With*`
      constructor is unchanged, but a hand-written `Option` closure will no longer compile. Options
      are now order-independent, so `WithChannels` before `WithMaxChannels` is no longer discarded.

      - **`meshcore.MACThenDecrypt` returns `ErrBadMAC`** on a MAC mismatch instead of `(nil, nil)`.
      Code branching on a nil error must now check the error.

      - **`companion.Cmd*`, `Resp*`, `StatsType*` and `AutoAdd*` constants are typed `byte`**
      instead of untyped ints. Assignments to other integer types now need a conversion.

      - **`companion.Err*` error codes are renamed `ErrCode*`** because they are protocol bytes, not
      Go errors. The old names remain as deprecated aliases.

      - **`Client.SendChannelTextMessage` returns an empty `SentResponse`** when the firmware
      answers OK, which it does today. Check `HasAckCode` or `HasExtended` before reading the
      fields.

      - **`Client.OnPush` now returns an unsubscribe function.** Existing calls still compile if
      they ignore the result.

      - **`hardware.ErrTxBusy` is never returned.** Since firmware 1.17 the modem also raises
      `HW_ERR_TX_BUSY` for host-output backpressure, so it no longer identifies a rejected transmit.
      The sentinel stays exported and deprecated.

      - **`hardware.KissModem.Flush` is test-only** and no longer available to external callers.


      ## Interoperability fixes


      **Companion**


      - `CompanionModem` sent node packets with `CMD_SEND_RAW_DATA`, which the firmware wraps in a
      new zero-hop `RAW_CUSTOM` packet, so no MeshCore node could decode or relay them. It now uses
      `CMD_SEND_RAW_PACKET`. It also takes inbound packets from `PushLogRxData` only; `PushRawData`
      carries a bare payload rather than a packet and was being handed to the node as one, and
      delivered twice.

      - Six calls waited for the wrong reply and timed out even though the firmware had executed the
      command. Login, status, telemetry, binary and trace requests are acknowledged with
      `RESP_CODE_SENT`; channel text answers a plain OK.

      - `AddUpdateContact` sent a 65-byte frame where the firmware reads 136, so contact type, flags
      and route were parsed out of the name bytes. `AddUpdateContactFull` exposes every stored
      field.

      - Channel datagrams could never flood, because the firmware floods only on `path_len` `0xFF`.
      Use the new `SendChannelDataFlood`, or the `Flood` field on the command.

      - `GetWaitingMessages` now drains `RESP_CODE_CHANNEL_DATA_RECV`, which previously wedged the
      sync loop with the datagram stuck at the head of the queue.

      - Advert-path, path-discovery and trace parsers decode the encoded `path_len` byte rather than
      treating it as a raw count, so multi-byte hop hashes no longer fail to parse. Trace SNR bytes
      are counted as `path_len >> (flags & 3)`.

      - Signed plain text exposes its 4-byte sender prefix separately instead of leaving it at the
      front of the message body.

      - Device info, login success and end-of-contacts now expose the fields that were being d

      …
  - version: v1.1.0
    name: v1.1.0
    datetime: '2026-08-24T03:10:03Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.1.0
    prerelease: false
    notes: >-
      ## What's Changed

      * release: v1.1.0 — MeshCore expanded private key support by @tuzzmaniandevil in
      https://github.com/meshcore-go/meshcore-go/pull/11



      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.9...v1.1.0
  - version: v1.0.9
    name: v1.0.9
    datetime: '2026-06-28T23:14:47Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.9
    prerelease: false
    notes: >-
      ## Highlights


      ### CayenneLPP Polyline (type 240)

      Full Polyline codec, byte-compatible with
      [ElectronicCats/CayenneLPP](https://github.com/ElectronicCats/CayenneLPP):

      - `AddPolyline` / `AddPolylineFactor` encoders — Douglas-Peucker & perpendicular-distance
      simplification, error-feedback delta quantization, signed-nibble packing.

      - Variable-length polyline decoding in `LPPDecode`, new `LPPCoordinate` / `LPPPolylineValue`
      types and `LPPPrecision` / `LPPSimplification` enums.

      - `NewLPPEncoderSize` to match a specific `CayenneLPP(size)` buffer.

      - Output verified byte-identical to the C++ reference on every input it can encode; also
      handles two inputs that crash the upstream library.


      ### Routing fix

      - `node`: 0-hop direct neighbours now route **direct** instead of flooding — a non-nil
      zero-length path is a known route, so we test `path != nil` rather than `len(path) > 0`.


      ### Other

      - Decode end-marker aligned to MeshCore's `LPPReader` (stop on `channel == 0`, any type).

      - `go.work.sum` cleanup; submodule deps bumped to v1.0.9.


      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.8...v1.0.9
  - version: v1.0.8
    name: v1.0.8
    datetime: '2026-06-12T01:39:40Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.8
    prerelease: false
    notes: "# meshcore-go — v1.0.8\r\n\r\nMostly about getting us in line with MeshCore 1.16. There's one breaking change\r\n(SNR is now float32), so callers will need a small update.\r\n\r\n## 1.16 protocol\r\n\r\n- Companion serial protocol updated for 1.16: added `CMD_SEND_RAW_PACKET` (65),\r\n  default flood scope get/set (63/64, RESP 28) and the unscoped-flood sub-type,\r\n  and new push codes for LoginFail, ContactDeleted and ContactsFull.\r\n- Bumped MaxFrameSize 172 → 176 and now advertise protocol v3 so the firmware\r\n  sends V3 message-recv frames.\r\n- Renamed a few Companion constants to match firmware: `CmdSetRadioTxPower`,\r\n  `CmdGetBattAndStorage`, `CmdSetFloodScopeKey`, `RespBattAndStorage`.\r\n- ACKs now dedup by packet hash like everything else (1.16 dropped the separate\r\n  ACK-CRC table). Retransmitted ACKs get forwarded again instead of being\r\n  silently dropped.\r\n- KISS: treat a `TX_DONE` result of `0x00` as a failed transmit. Firmware 1.16\r\n  sends that on radio-busy / airtime timeout. Baseline is now 1.16+.\r\n- Text retransmits are recomposed per attempt (attempt encoded in the flags\r\n  byte, plus a `[0x00][attempt]` tail past attempt 3) so each retry has a unique\r\n  hash and survives dedup. ACK CRC is recomputed each attempt. Added\r\n  `BuildTextPlaintextWithAttempt`.\r\n\r\n## Other changes\r\n\r\n- SNR is now real dB (`float32`) everywhere instead of raw quarter-dB `int8`.\r\n  RSSI is unchanged and the raw path-SNR bytes are still preserved. **This is\r\n  breaking** — see below.\r\n- Added TX stats: `Node.TxStats()` returns a `TxStats` struct, plumbed through\r\n  the transport/modem/tx-engine layers.\r\n- Added `HasSignalInfo` through the modem/transport/peer path so we only apply\r\n  SNR/RSSI when the source actually has it (helps the TCP transport).\r\n- Peers can now carry multi-byte path hashes via `OutPathHashSize` (1, 2 or 4;\r\n  0 means default of 1); `SetOutPath` takes an optional hash size.\r\n- New `WithLearnedPathsOnly()` option to restrict advert paths to learned ones.\r\n- Name truncation now respects UTF-8 boundaries.\r\n\r\n## Breaking\r\n\r\n- SNR fields went `int8` → `float32` (real dB). `PeerTable.Update` is now\r\n  `Update(adv, snr float32, rssi int8, hasSignalInfo bool, pathHashes []byte)` —\r\n  update the call sites and stop treating SNR as raw quarter-dB."
  - version: v1.0.7
    name: v1.0.7
    datetime: '2026-06-12T01:21:32Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.7
    prerelease: false
    notes: "## What's Changed\r\n* v1.0.7 by @tuzzmaniandevil in https://github.com/meshcore-go/meshcore-go/pull/8\r\n\r\nDO NOT USE, Use 1.0.8 instead\r\n\r\n**Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.6...v1.0.7"
  - version: v1.0.6
    name: v1.0.6
    datetime: '2026-05-10T23:40:26Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.6
    prerelease: false
    notes: >-
      ## What's Changed

      * Refactor transmission handling and update dependencies by @tuzzmaniandevil in
      https://github.com/meshcore-go/meshcore-go/pull/7



      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.5...v1.0.6
  - version: v1.0.5
    name: v1.0.5
    datetime: '2026-05-06T23:49:25Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.5
    prerelease: false
    notes: >-
      ## What's Changed

      * Enhance TX flow control and add LoRa airtime estimation features by @tuzzmaniandevil in
      https://github.com/meshcore-go/meshcore-go/pull/6



      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.4...v1.0.5
  - version: v1.0.4
    name: v1.0.4
    datetime: '2026-05-04T01:13:55Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.4
    prerelease: false
    notes: >-
      ## What's Changed

      * Implement packet retry mechanism and enhance radio transmission queuing by @tuzzmaniandevil
      in https://github.com/meshcore-go/meshcore-go/pull/4

      * v1.0.4 by @tuzzmaniandevil in https://github.com/meshcore-go/meshcore-go/pull/5



      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.3...v1.0.4
  - version: v1.0.3
    name: v1.0.3
    datetime: '2026-04-30T23:24:59Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.3
    prerelease: false
    notes: |-
      ## What's Changed
      * v1.0.3 by @tuzzmaniandevil in https://github.com/meshcore-go/meshcore-go/pull/3


      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.2...v1.0.3
  - version: v1.0.2
    name: v1.0.2
    datetime: '2026-04-29T23:43:32Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.2
    prerelease: false
    notes: |-
      ## What's Changed
      * v1.0.2 by @tuzzmaniandevil in https://github.com/meshcore-go/meshcore-go/pull/2


      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.1...v1.0.2
  - version: v1.0.1
    name: v1.0.1
    datetime: '2026-04-29T09:46:52Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.1
    prerelease: false
    notes: >-
      ## What's Changed

      * v1.0.1 by @tuzzmaniandevil in https://github.com/meshcore-go/meshcore-go/pull/1


      ## New Contributors

      * @tuzzmaniandevil made their first contribution in
      https://github.com/meshcore-go/meshcore-go/pull/1


      **Full Changelog**: https://github.com/meshcore-go/meshcore-go/compare/v1.0.0...v1.0.1
  - version: v1.0.0
    name: v1.0.0
    datetime: '2026-04-28T03:11:35Z'
    url: https://github.com/meshcore-go/meshcore-go/releases/tag/v1.0.0
    prerelease: false
    notes: '**Full Changelog**: https://github.com/meshcore-go/meshcore-go/commits/v1.0.0'
